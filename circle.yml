
machine:
  java:
    version: oraclejdk7

general:
  branches:
    only:
    - master

test:
    post:
        - if [ -e ./gradlew ]; then sudo ./gradlew codeCoverageReport;else sudo gradle codeCoverageReport;fi
        - sudo bash <(curl -s https://codecov.io/bash)
