
/**
 * Copyright (C) 2017  Ardika Rommy Sanjaya
 */

description = "Module for crafting network packet."

dependencies {
	compile project(':jxnet-core')
	compile project(':jxnet-common')
	compile project(':jxnet-util')
	testCompile "org.slf4j:slf4j-api:${SLF4J_VERSION}"
	testCompile "ch.qos.logback:logback-core:${LOGBACK_VERSION}"
	testCompile "ch.qos.logback:logback-classic:${LOGBACK_VERSION}"
	testCompile "junit:junit:${JUNIT_VERSION}"
}

clean {
	file("${rootDir}/jxnet-packet/obj").deleteDir()
	file("${rootDir}/jxnet-packet/out").deleteDir()
}

test {
}
