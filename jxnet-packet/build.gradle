
dependencies {
	compile project(":jxnet-core")
	testCompile "junit:junit:${JUNIT_VERSION}"
}

clean {
	file("${rootDir}/jxnet-packet/obj").deleteDir()
	file("${rootDir}/jxnet-packet/out").deleteDir()
}

